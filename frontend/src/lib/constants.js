// Single source for contact details so the address cannot drift between the
// landing page, the auth screen and the in-app help links.
export const SUPPORT_EMAIL = "lyvoranlr@gmail.com";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
