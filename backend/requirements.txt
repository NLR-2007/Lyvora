# Pinned to exact versions so a rebuild reproduces the environment that was
# actually tested. Bump deliberately, not implicitly on the next install.

fastapi==0.138.1
uvicorn[standard]==0.49.0
playwright==1.60.0
sqlalchemy==2.0.51
alembic==1.18.5
mysql-connector-python==9.7.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
httpx==0.28.1
# bcrypt is imported directly by auth.py and database.py. passlib was declared
# here instead but never imported — and passlib 1.7.4 is broken against
# bcrypt 5.x, so keeping it only invited a runtime failure.
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
email-validator==2.3.0
cryptography==49.0.0
# Optional: /api/admin/health degrades gracefully when this is absent.
psutil==7.1.2
tzdata==2026.3
