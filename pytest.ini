[pytest]
testpaths = backend/tests
# Backend modules import as `backend.*`, so the repo root must be on sys.path.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
